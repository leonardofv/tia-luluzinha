from django.db import models
'''
from django.contrib.auth.models import AbstractUser
from boletins.models import Aluno

class CustomUser(AbstractUser):
    matricula = models.CharField(max_length=10)
'''